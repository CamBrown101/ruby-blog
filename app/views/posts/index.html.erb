<h1>All Posts</h1>

<table>
  <thead>
    <th>Title</th>
    <th>Body</th>
    <th>Delete</th>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <td><%= link_to "Delete", post_path(post), method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>